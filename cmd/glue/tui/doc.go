// Package tui implements the interactive multi-turn terminal interface
// for `glue run`. It builds on the standard glue.Agent / glue.Session /
// glue.Permission interfaces — the agent loop is unaware the host
// happens to be a TUI. See docs/adr/0014-coding-agent-tui.md.
package tui
